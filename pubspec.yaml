name: realfun_attendance
description: A new Flutter project.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ---- UI / UX ----
  google_fonts: ^6.1.0
  auto_size_text: ^3.0.0
  flutter_animate: ^4.5.0
  page_transition: ^2.1.0
  cached_network_image: ^3.4.1

  # ---- State / Routing ----
  provider: ^6.1.5
  go_router: ^12.1.3

  # ---- Utilities (optional but handy for UI demos) ----
  intl: ^0.20.2
  url_launcher: ^6.3.1
  shared_preferences: ^2.5.3
  image_picker: ^1.1.2
  path_provider: ^2.1.4
  sqflite: ^2.3.3+1          # keep only if you plan local DB demos
  supabase_auth_ui: ^0.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/images/
